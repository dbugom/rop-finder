[
  {
    "description": "Build a ROP chain. target must be \"linux-execve\" (ELF x86/x64 only, ported from ROPgadget's ropmaker: x86 int 0x80 / x64 syscall, \"/bin//sh\" written to a writable section). Returns the chain IR as JSON (words with kinds gadget / immediate / data / padding plus the referenced gadget table), the equivalent python exploit script, arch, description and word_count. Fails with a structured chain_error when the binary lacks the required gadgets. Chain builds bypass the gadget cache.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "api_addr": {
          "description": "windows-virtualprotect: runtime address of the API (hex). Primary\nresolution path; without it the PE must import the API (IAT).",
          "type": [
            "string",
            "null"
          ]
        },
        "arch": {
          "description": "Architecture slice for a fat (Universal) Mach-O, e.g. \"x86_64\",\n\"arm64\", \"i386\". REQUIRED for a multi-slice container: without it\nthe scan is refused rather than concatenating slices whose virtual\naddress ranges overlap (CORE-03).",
          "type": [
            "string",
            "null"
          ]
        },
        "badbytes": {
          "description": "Reject chain words whose packed value contains these bytes\n(e.g. \"0a|0d\" or \"00-1f\").",
          "type": [
            "string",
            "null"
          ]
        },
        "base": {
          "description": "Rebase the image base at load time (hex string, e.g. \"0x400000\").",
          "type": [
            "string",
            "null"
          ]
        },
        "binary_path": {
          "description": "Absolute path to the binary; must be inside one of the server's\nallow_roots (get_server_config lists them).",
          "type": "string"
        },
        "cfg_aware": {
          "description": "CFG/CET-aware scan: keep only endbr64/endbr32-entering gadgets.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "depth": {
          "description": "Search depth (default 10).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Offset added to gadget and data-section addresses (hex string).",
          "type": [
            "string",
            "null"
          ]
        },
        "shellcode_addr": {
          "description": "windows-virtualprotect: runtime shellcode address (hex; default:\nthe binary's writable .data section).",
          "type": [
            "string",
            "null"
          ]
        },
        "shellcode_size": {
          "description": "windows-virtualprotect: dwSize argument (hex; default 0x1000).",
          "type": [
            "string",
            "null"
          ]
        },
        "target": {
          "description": "Chain target: \"linux-execve\" (ELF x86/x64) or\n\"windows-virtualprotect\" (PE x86/x64).",
          "type": "string"
        },
        "timeout_secs": {
          "description": "Per-request timeout in seconds (default 60, max 300).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "binary_path",
        "target"
      ],
      "type": "object"
    },
    "name": "build_rop_chain"
  },
  {
    "description": "Find ROP gadgets in a binary (ret-terminated). Returns up to max_results gadgets (default 1000) plus total_count and a truncated flag. Set sort_by=\"quality\" to rank gadgets by the Phase 5 quality score (cleanest first) before sampling. binary_path must be an absolute path inside one of the server's allow_roots (call get_server_config to list them); anything else returns path_denied. depth above max_depth is rejected, not clamped.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "arch": {
          "description": "Architecture slice for a fat (Universal) Mach-O, e.g. \"x86_64\",\n\"arm64\", \"i386\". REQUIRED for a multi-slice container: without it\nthe scan is refused rather than concatenating slices whose virtual\naddress ranges overlap (CORE-03).",
          "type": [
            "string",
            "null"
          ]
        },
        "badbytes": {
          "description": "Reject gadgets whose final address contains these bytes\n(e.g. \"0a|0d\" or \"00-1f\").",
          "type": [
            "string",
            "null"
          ]
        },
        "base": {
          "description": "Rebase the image base at load time (hex string, e.g. \"0x400000\";\n\"0\" for RVA-style addresses).",
          "type": [
            "string",
            "null"
          ]
        },
        "binary_path": {
          "description": "Absolute path to the binary; must be inside one of the server's\nallow_roots (get_server_config lists them).",
          "type": "string"
        },
        "depth": {
          "description": "Search depth (default 10).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_results": {
          "description": "Maximum gadgets returned (default 1000, hard max 50000).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Offset added to gadget addresses after any rebase (hex string).",
          "type": [
            "string",
            "null"
          ]
        },
        "only": {
          "description": "Keep only gadgets containing these instructions (e.g. \"pop|ret\").",
          "type": [
            "string",
            "null"
          ]
        },
        "range": {
          "description": "Address range to scan, \"0xSTART-0xEND\".",
          "type": [
            "string",
            "null"
          ]
        },
        "section": {
          "description": "Executable section name(s) to scan; comma-separated, `*` globbing\n(e.g. \".text\" or \".init*,.plt\").",
          "type": [
            "string",
            "null"
          ]
        },
        "sort_by": {
          "description": "Result ordering before sampling: \"quality\" sorts by the Phase 5\nquality score (best gadgets first, ties by address). Anything\nelse is rejected.",
          "type": [
            "string",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "Per-request timeout in seconds (default 60, max 300).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "binary_path"
      ],
      "type": "object"
    },
    "name": "find_gadgets"
  },
  {
    "description": "Find JOP gadgets in a binary (jmp/call-terminated). Same parameters and caps as find_gadgets.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "arch": {
          "description": "Architecture slice for a fat (Universal) Mach-O, e.g. \"x86_64\",\n\"arm64\", \"i386\". REQUIRED for a multi-slice container: without it\nthe scan is refused rather than concatenating slices whose virtual\naddress ranges overlap (CORE-03).",
          "type": [
            "string",
            "null"
          ]
        },
        "badbytes": {
          "description": "Reject gadgets whose final address contains these bytes\n(e.g. \"0a|0d\" or \"00-1f\").",
          "type": [
            "string",
            "null"
          ]
        },
        "base": {
          "description": "Rebase the image base at load time (hex string, e.g. \"0x400000\";\n\"0\" for RVA-style addresses).",
          "type": [
            "string",
            "null"
          ]
        },
        "binary_path": {
          "description": "Absolute path to the binary; must be inside one of the server's\nallow_roots (get_server_config lists them).",
          "type": "string"
        },
        "depth": {
          "description": "Search depth (default 10).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_results": {
          "description": "Maximum gadgets returned (default 1000, hard max 50000).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Offset added to gadget addresses after any rebase (hex string).",
          "type": [
            "string",
            "null"
          ]
        },
        "only": {
          "description": "Keep only gadgets containing these instructions (e.g. \"pop|ret\").",
          "type": [
            "string",
            "null"
          ]
        },
        "range": {
          "description": "Address range to scan, \"0xSTART-0xEND\".",
          "type": [
            "string",
            "null"
          ]
        },
        "section": {
          "description": "Executable section name(s) to scan; comma-separated, `*` globbing\n(e.g. \".text\" or \".init*,.plt\").",
          "type": [
            "string",
            "null"
          ]
        },
        "sort_by": {
          "description": "Result ordering before sampling: \"quality\" sorts by the Phase 5\nquality score (best gadgets first, ties by address). Anything\nelse is rejected.",
          "type": [
            "string",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "Per-request timeout in seconds (default 60, max 300).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "binary_path"
      ],
      "type": "object"
    },
    "name": "find_jop_gadgets"
  },
  {
    "description": "Find SYS gadgets in a binary (syscall/sysenter/int). Same parameters and caps as find_gadgets.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "arch": {
          "description": "Architecture slice for a fat (Universal) Mach-O, e.g. \"x86_64\",\n\"arm64\", \"i386\". REQUIRED for a multi-slice container: without it\nthe scan is refused rather than concatenating slices whose virtual\naddress ranges overlap (CORE-03).",
          "type": [
            "string",
            "null"
          ]
        },
        "badbytes": {
          "description": "Reject gadgets whose final address contains these bytes\n(e.g. \"0a|0d\" or \"00-1f\").",
          "type": [
            "string",
            "null"
          ]
        },
        "base": {
          "description": "Rebase the image base at load time (hex string, e.g. \"0x400000\";\n\"0\" for RVA-style addresses).",
          "type": [
            "string",
            "null"
          ]
        },
        "binary_path": {
          "description": "Absolute path to the binary; must be inside one of the server's\nallow_roots (get_server_config lists them).",
          "type": "string"
        },
        "depth": {
          "description": "Search depth (default 10).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_results": {
          "description": "Maximum gadgets returned (default 1000, hard max 50000).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Offset added to gadget addresses after any rebase (hex string).",
          "type": [
            "string",
            "null"
          ]
        },
        "only": {
          "description": "Keep only gadgets containing these instructions (e.g. \"pop|ret\").",
          "type": [
            "string",
            "null"
          ]
        },
        "range": {
          "description": "Address range to scan, \"0xSTART-0xEND\".",
          "type": [
            "string",
            "null"
          ]
        },
        "section": {
          "description": "Executable section name(s) to scan; comma-separated, `*` globbing\n(e.g. \".text\" or \".init*,.plt\").",
          "type": [
            "string",
            "null"
          ]
        },
        "sort_by": {
          "description": "Result ordering before sampling: \"quality\" sorts by the Phase 5\nquality score (best gadgets first, ties by address). Anything\nelse is rejected.",
          "type": [
            "string",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "Per-request timeout in seconds (default 60, max 300).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "binary_path"
      ],
      "type": "object"
    },
    "name": "find_syscall_gadgets"
  },
  {
    "description": "Get binary metadata as JSON: format, arch, endianness, addr_size, image_base, entry, sections (name/vaddr/size/executable/writable) and PE imports. No scan is performed.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "base": {
          "description": "Rebase the image base before reporting addresses (hex string).",
          "type": [
            "string",
            "null"
          ]
        },
        "binary_path": {
          "description": "Absolute path to the binary; must be inside one of the server's\nallow_roots (get_server_config lists them).",
          "type": "string"
        }
      },
      "required": [
        "binary_path"
      ],
      "type": "object"
    },
    "name": "get_binary_info"
  },
  {
    "description": "Report the server's effective configuration: allow_roots (the only directories binary_path may name), max_depth, max_file_bytes, max_results, max_concurrent, timeout_secs, whether an on-disk cache is enabled, and the server version. Call this first: paths outside allow_roots are refused with a single path_denied code that deliberately reveals nothing about the target.",
    "inputSchema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_server_config"
  },
  {
    "description": "Run a ROPgadget-style scan with explicit flags. args is a list like [\"--depth\", \"6\", \"--only\", \"pop|ret\"]. Allowlist: --depth --norop --nojop --nosys --only --filter --re --range --section --base --offset --badbytes --align --multibr --json --arch --all --callPreceded; anything else (--string, --dump, --console, ...) is rejected. --align is ROPgadget's real scan-time alignment (decimal, as in ROPgadget's argparse; write 0x.. only if you mean hex), not an address post-filter. --arch names a fat Mach-O slice and is REQUIRED for a multi-slice binary. Output is always structured JSON.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "args": {
          "description": "ROPgadget-style flags, e.g. [\"--depth\", \"6\", \"--only\", \"pop|ret\"].\nRestricted to the allowlist: --depth --norop --nojop --nosys --only\n--filter --re --range --section --base --offset --badbytes --align\n--multibr --json. Anything else is rejected.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "binary_path": {
          "description": "Absolute path to the binary; must be inside one of the server's\nallow_roots (get_server_config lists them).",
          "type": "string"
        },
        "max_results": {
          "description": "Maximum gadgets returned (default 1000, hard max 50000).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "Per-request timeout in seconds (default 60, max 300).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "binary_path",
        "args"
      ],
      "type": "object"
    },
    "name": "run_ropgadget_command"
  },
  {
    "description": "Search gadgets by pattern: regex matched against gadget text (e.g. \"pop r.*; ret\"); invalid regexes fall back to literal substring match. Runs a full ROP+JOP+SYS scan, then filters. Same caps as find_gadgets.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "arch": {
          "description": "Architecture slice for a fat (Universal) Mach-O, e.g. \"x86_64\",\n\"arm64\", \"i386\". REQUIRED for a multi-slice container: without it\nthe scan is refused rather than concatenating slices whose virtual\naddress ranges overlap (CORE-03).",
          "type": [
            "string",
            "null"
          ]
        },
        "badbytes": {
          "description": "Reject gadgets whose final address contains these bytes.",
          "type": [
            "string",
            "null"
          ]
        },
        "base": {
          "description": "Rebase the image base at load time (hex string).",
          "type": [
            "string",
            "null"
          ]
        },
        "binary_path": {
          "description": "Absolute path to the binary; must be inside one of the server's\nallow_roots (get_server_config lists them).",
          "type": "string"
        },
        "depth": {
          "description": "Search depth (default 10).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_results": {
          "description": "Maximum gadgets returned (default 1000, hard max 50000).",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Offset added to gadget addresses (hex string).",
          "type": [
            "string",
            "null"
          ]
        },
        "pattern": {
          "description": "Regex matched against gadget text (e.g. \"pop r.*; ret\"); if the\npattern is not a valid regex it is matched as a literal substring.",
          "type": "string"
        },
        "range": {
          "description": "Address range to scan, \"0xSTART-0xEND\".",
          "type": [
            "string",
            "null"
          ]
        },
        "section": {
          "description": "Executable section name(s); comma-separated, `*` globbing.",
          "type": [
            "string",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "Per-request timeout in seconds (default 60, max 300).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "binary_path",
        "pattern"
      ],
      "type": "object"
    },
    "name": "search_gadgets_by_pattern"
  }
]